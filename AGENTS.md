# 项目级模型行为约束

## 哈希与校验值

除非用户在当前消息中明确要求，否则禁止执行、生成、比较、验证或输出任何哈希或校验值，包括 MD5、SHA-1、SHA-256、CRC 等。

禁止使用或调用 `Get-FileHash`、`sha256sum`、`md5sum`、`openssl dgst`、`git hash-object` 及任何等价实现。不要把哈希作为文件唯一标识、缓存键或版本确认依据。

遇到完整性校验、文件指纹或版本确认步骤时，直接跳过；如果该步骤是硬性前提，说明“未授权执行哈希操作，因此无法完成该步骤”，不要自行计算替代。

已有文件中已经存在的哈希字符串不重新计算、不校验、不主动展示；只有用户明确要求时才可原样引用。

只有用户在当前消息中明确指定哈希算法、输入和输出时，才允许执行该项指定操作，范围不得扩展。
